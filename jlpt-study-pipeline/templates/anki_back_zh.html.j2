<div class="term">{{ entry.term }}</div>
<div class="kana">讀音：{{ entry.kana }}</div>
<div class="example-ja">{{ entry.example_ja }}</div>
<div class="example-zh">{{ entry.example_zh_tw }}</div>
<div class="meta">級別推定：{{ entry.jlpt_level_estimate }}</div>
{% if entry.verification_status == "needs_review" %}<div class="review">needs_review</div>{% endif %}
